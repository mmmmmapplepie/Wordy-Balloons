using TMPro;
using UnityEngine;

public class FocusKeyboard : MonoBehaviour {
	public InputManager inputManager;
	public TMP_InputField inputField;
	void Update() {
		// if (!inputManager.canTakeInput) return;
		// if (Input.anyKeyDown) {
		// 	inputField.ActivateInputField();
		// }
	}
}
